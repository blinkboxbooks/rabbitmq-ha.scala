service: {
  test: {
    testQueue {
      queueName: "headers test queue name",
      exchangeName: "headers test exchange name",
      routingKeys: [], // routing key and queueBindingArguments are mutually exclusive
      prefetchCount: 42,
      exchangeType: "headers"
      messageTimeout: 10 seconds,
      bindingArguments: {
        app_id: "test-app-1"
      }
    }
  }
}
