service : {
  test : {
    testQueue {
        queueName : "test queue name",
        exchangeName : "test exchange name",
        routingKeys : ["test key 1", "test key 2", "test key 3"], // routing key and queueBindingArguments are mutually exclusive
        prefetchCount : 42,
        exchangeType : "topic"
        messageTimeout : 10 seconds
    }
  }
}
