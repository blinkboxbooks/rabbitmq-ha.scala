service : {
  test : {
    testQueue {
        queueName : "test queue name",
        exchangeName : "test exchange name",
        exchangeType : "headers"
        prefetchCount : 1000,
        bindingArguments: { "foo" : "1", "bar" : 2 }
    }
  }
}
